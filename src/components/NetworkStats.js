import React from 'react';

// Legacy component - functionality has been split into multiple components
// Keeping for backwards compatibility
const NetworkStats = ({ api }) => {
  return null;
};

export default NetworkStats;
